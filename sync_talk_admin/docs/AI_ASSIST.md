
# AI Assist
- `/api/ai/suggest` takes a compact prompt derived from last messages and returns a suggestion.
- Mobile "✨" button fetches suggestion and inserts into composer.
- Use ENV key to switch between stub and real provider.
