
# Contributing Guidelines
- Use feature branches and meaningful commits.
- Keep PRs small; include screenshots for UI changes.
- Run CI locally where possible (`npm test`, `flutter analyze`, `flutter test`).
